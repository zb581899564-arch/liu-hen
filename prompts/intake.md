# 入口确认

先确认 3 件事，尽量简短回答：

1. 这批数据是什么形态？
   - 可能是导出的结构化 JSON
   - 也可能是聊天记录、片段或混合文本

2. 角色怎么定？
   - `target` = 需要模仿的对象
   - `user` = 和 `target` 互动的上下文来源

3. 这次要输出什么？
   - 记忆
   - 人设
   - 关系上下文
   - 回复模式
   - sticker / emoji / 标点习惯

如果输入已经是 JSON，直接按字段读；如果不是，就先按原始材料抽取，再补结构。

